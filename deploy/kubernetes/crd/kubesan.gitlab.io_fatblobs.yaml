# SPDX-License-Identifier: Apache-2.0

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: fatblobs.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    kind: FatBlob
    listKind: FatBlobList
    plural: fatblobs
    singular: fatblob
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              attachToNodes:
                description: May be updated at will.
                items:
                  type: string
                type: array
              readOnly:
                description: Should be set from creation and never updated.
                type: boolean
              sizeBytes:
                description: Must be positive and a multiple of 512. May be updated
                  at will, but the actual size will only ever increase.
                format: int64
                type: integer
              vgName:
                description: Should be set from creation and never updated.
                type: string
            required:
            - readOnly
            - sizeBytes
            - vgName
            type: object
          status:
            properties:
              attachedToNodes:
                description: The nodes to which the blob is attached.
                items:
                  type: string
                type: array
              created:
                description: Whether the blob has been created.
                type: boolean
              path:
                description: The path at which the blob is available on nodes to which
                  it is attached.
                type: string
              sizeBytes:
                description: The current size of the blob.
                format: int64
                type: integer
            required:
            - created
            - sizeBytes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
