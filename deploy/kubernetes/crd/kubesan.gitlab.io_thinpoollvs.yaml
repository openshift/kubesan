# SPDX-License-Identifier: Apache-2.0

# Code generated by controller-gen. DO NOT EDIT.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: thinpoollvs.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    kind: ThinPoolLv
    listKind: ThinPoolLvList
    plural: thinpoollvs
    singular: thinpoollv
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              activeOnNode:
                description: |-
                  Name of node where activation is needed, or empty.
                  When changing, may only toggle between "" and non-empty.
                type: string
                x-kubernetes-validations:
                - rule: (oldSelf==self)||((oldSelf=="")!=(self==""))
              sharing:
                description: Whether NBD sharing is needed from the active node.
                enum:
                - NotNeeded
                - ServeNBD
                type: string
              thinLvs:
                description: May be updated at will.
                items:
                  properties:
                    activate:
                      description: May be updated at will.
                      type: boolean
                    contents:
                      description: Should be set from creation and never updated.
                      properties:
                        empty:
                          description: The LVM thin LV should initially be zeroed
                            out.
                          type: object
                        snapshot:
                          description: The LVM thin LV should initially be a copy
                            of another LVM thin LV.
                          properties:
                            sourceThinLvName:
                              type: string
                          required:
                          - sourceThinLvName
                          type: object
                      type: object
                    name:
                      description: Should be set from creation and never updated.
                      type: string
                    readOnly:
                      description: Should be set from creation and never updated.
                      type: boolean
                    sizeBytes:
                      description: |-
                        Must be positive and a multiple of 512. May be updated at will, but the LVM thin LV's actual size will only
                        ever increase.
                      format: int64
                      type: integer
                  required:
                  - activate
                  - contents
                  - name
                  - readOnly
                  - sizeBytes
                  type: object
                type: array
              vgName:
                description: Should be set from creation and never updated.
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - sharing
            - vgName
            type: object
            x-kubernetes-validations:
            - rule: self.activeOnNode!=""||self.sharing!="ServeNBD"
          status:
            properties:
              activeOnNode:
                description: The name of the node where the LVM thin pool LV is active,
                  along with any active LVM thin LVs; or "".
                type: string
              conditions:
                description: |-
                  Conditions
                  Available: The LVM volume has been created
                  Activated: The last time Status.ActiveOnNode changed
                items:
                  description: |-
                    Condition represents the state of the operator's
                    reconciliation functionality.
                  properties:
                    lastHeartbeatTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      description: ConditionType is the state of the operator's reconciliation
                        functionality.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              nbdSever:
                description: The name of a Pod serving NBD, or "" if not available
                type: string
              thinLvs:
                description: The status of each LVM thin LV that currently exists
                  in the LVM thin pool LV.
                items:
                  properties:
                    name:
                      description: The name of the LVM thin LV.
                      type: string
                    sizeBytes:
                      description: The current size of the LVM thin LV.
                      format: int64
                      type: integer
                    state:
                      description: The state of the LVM thin LV.
                      properties:
                        active:
                          description: The LVM thin LV is active on the node where
                            the LVM thin pool LV is active.
                          properties:
                            path:
                              description: The path at which the LVM thin LV is available
                                on the node where the LVM thin pool LV is active.
                              type: string
                          required:
                          - path
                          type: object
                        inactive:
                          description: The LVM thin LV is not active on any node.
                          type: object
                      type: object
                  required:
                  - name
                  - sizeBytes
                  - state
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
