# SPDX-License-Identifier: Apache-2.0

# Code generated by controller-gen. DO NOT EDIT.

---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: snapshots.kubesan.gitlab.io
spec:
  group: kubesan.gitlab.io
  names:
    categories:
    - kubesan
    - lv
    kind: Snapshot
    listKind: SnapshotList
    plural: snapshots
    shortNames:
    - snap
    - snaps
    singular: snapshot
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: '''VG owning the snapshot'''
      jsonPath: .spec.vgName
      name: VG
      type: string
    - description: '''Volume that this snapshot was created on'''
      jsonPath: .spec.sourceVolume
      name: Source
      type: string
    - description: '''Time since snapshot was available'''
      jsonPath: .status.conditions[?(@.type=="Available")].lastTransitionTime
      name: Available
      type: date
    - description: '''Size of snapshot'''
      jsonPath: .status.sizeBytes
      name: Size
      type: integer
    - description: '''filesystem type (blank if block)'''
      jsonPath: .status.fsType
      name: FSType
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              sourceVolume:
                description: Should be set from creation and never updated.
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              vgName:
                description: Should be set from creation and never updated.
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - sourceVolume
            - vgName
            type: object
          status:
            properties:
              conditions:
                description: |-
                  Conditions
                  Available: The snapshot can be sourced by volumes.
                items:
                  description: |-
                    Condition represents the state of the operator's
                    reconciliation functionality.
                  properties:
                    lastHeartbeatTime:
                      format: date-time
                      type: string
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      description: ConditionType is the state of the operator's reconciliation
                        functionality.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              fsType:
                description: The file system type of the snapshot. `nil` if a snapshot
                  of a block volume.
                type: string
                x-kubernetes-validations:
                - rule: oldSelf==self
              observedGeneration:
                description: |-
                  The generation of the spec used to produce this status.  Useful
                  as a witness when waiting for status to change.
                format: int64
                type: integer
              sizeBytes:
                description: The size of the snapshot, immutable once set.
                format: int64
                type: integer
                x-kubernetes-validations:
                - rule: oldSelf==self
            required:
            - observedGeneration
            - sizeBytes
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
